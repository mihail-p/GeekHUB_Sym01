{% extends '@App/AdminTeam/admTeamShowAll.html.twig' %}
{% block adm_cont %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    Submit<input type="submit" value="Submit" class="btn btn-default left"/>
    {{ form_end(form) }}
    <br>
    {{ dump(formData) }}
    <p>{{ dump(formString) }}</p>
    <p>{{ formString }}</p>
{% endblock %}